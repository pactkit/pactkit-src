# HOTFIX-slim-20260903d464746b1909: opencode 命令部署 digest 失记（候选跑步机 opencode 孪生）

| Field | Value |
|-------|-------|
| ID | HOTFIX-slim-20260903d464746b1909 |
| Status | Done |
| Priority | P1 |

## Background

opencode 适配器两段式部署（core 所有权检查写原始内容 → 适配器后处理改写为 CLI-free 变体）从不记录改写后的 digest——`.pactkit-deployed.json` 的 files 映射既不被 core 为 opencode 写、也不被适配器更新。每次 update：raw 渲染 ≠ 转换后 live → 所有权无法证明 → 候选（PM-20260903 的 opencode 孪生，断点不同：codex 是"账本在写部署不读"，opencode 是"改写后根本没人记账"）。

## Fix

- core `deploy_manifest.py` 新增公开 `record_deployed_file(root, relative)`：按磁盘现状更新 files 映射 digest
- pactkit-opencode `deployer.py` 两处后处理 pass 之后，对 skills/*/SKILL.md 与 commands/*.md 逐个 `record_deployed_file`——下次 update 所有权可证（expected==actual → 原地更新，零候选）

## Verification

- 适配器仓测试 + core 全量
- E2E：本机 update 两次，零 preserve、零候选（修复前必产 3 个）

## Files

- `src/pactkit/deploy_manifest.py`（core，公开 helper）
- `/Users/slim/workspaces/pactkit-opencode/src/pactkit_opencode/deployer.py`（后处理后记账）
