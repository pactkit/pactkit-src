# Lessons Learned

| Date | Lesson | Context |
|------|--------|---------|
| 2025-01 | Brand rename (ScafPy -> PactKit) touched 306 references — automate with `replace_all` | Migration |
| 2025-02 | Selective deployment needs cleanup of stale files when config changes | STORY-002 |
| 2025-02 | `pactkit.yaml` must be generated on first `init` with sensible defaults | STORY-001 |
| 2025-02 | Cross-session value comes from persistent artifacts (context.md, lessons.md), not from more rules — prompt changes are the cheapest high-impact mechanism | STORY-006 |
| 2025-02 | Adding a new command touches 3 files (config.py, commands.py, rules.py) plus count assertions in existing tests — keep count tests data-driven to reduce churn | STORY-007 |
| 2025-02 | Removing rules that overlap with LLM native behavior (55% token reduction) improves signal-to-noise — fewer rules = higher compliance on the ones that matter | STORY-008 |
| 2025-02 | Auto-merge new components via separate function (not load_config) preserves existing contract — exclude section in yaml handles user opt-out without version diffing | STORY-009 |
| 2025-02 | Release prep is a good time to catch stale numbers in docs — embed counts as tests to prevent future drift | STORY-010 |
| 2026-02 | Demoting commands to skills is a prompt-only refactor (no Python scripts needed for prompt-only skills) — but updating 25+ test files with hardcoded counts is the real cost; prefer data-driven assertions | STORY-011 |
| 2026-02 | Multi-repo docs sync is cheap via gh CLI + git clone/push — but tests reading deployed files (not source) can hide regressions until redeployment | STORY-012 |
| 2026-02 | Integrating an external MCP server is a prompt-only change — add conditional instructions to rules, skills, agents, and workflows; no runtime code needed. The conditional pattern (IF tool available → use it; ELSE → fallback) is now proven across 6 MCP integrations | STORY-013 |
| 2026-03 | Global rules (rules.py) and command playbooks (commands.py) must not independently trigger the same action (e.g., visualize) — add PDCA exemption clauses to rules so commands are the single authority during PDCA execution; rules serve only free-conversation mode | BUG-slim-002 |
| 2026-02 | Skill SKILL.md prompts must use absolute paths for script invocations — the LLM runs bash from project cwd, not the skill base directory; match the pattern already used in workflows.py and commands.py (~/.claude/skills/{name}/scripts/{script}.py) | BUG-001 |
| 2026-02 | Deploy-time path rewriting (template stays canonical, deployer rewrites at write time) is the correct pattern for multi-mode deployment — adding a simple `_rewrite_skills_prefix()` helper keeps templates DRY while supporting classic/plugin/marketplace modes with different path conventions | BUG-002 |
| 2026-02 | When iterating `ast.Import.names`, each alias must be processed individually — a `for` loop that overwrites a single variable silently drops all but the last import; collect into a list instead. Also deduplicate edges with a `seen` set to avoid Mermaid rendering issues | BUG-003 |
| 2026-02 | Dead code from refactors (`set(x)` as standalone expression) passes all tests because it's a no-op — use AST-based source inspection tests to catch dead code that linters miss in generated/deployed scripts | BUG-004 |
| 2026-02 | When two functions classify the same data (board stories), they must use the same logic — `archive_stories` used absence-of-todo as "done" while `_classify_story` required presence-of-done; align both to require `- [x]` for archival | BUG-005 |
| 2026-02 | Non-AI linters (regex/AST) must strip fenced code blocks before searching for headings — content inside ``` blocks that looks like `## Section` will shadow the real section and produce false positives; preprocess text once at the top level and pass stripped version to all helpers | STORY-042 |
| 2026-02 | Subprocess-based CLI tests must use absolute paths — when another test changes CWD, relative paths like `src/pactkit/skills/spec_linter.py` break; use `Path(__file__).parents[2] / "src/..."` to anchor to project root | STORY-042 |
| 2026-02 | Parallel agent implementation of independent prompt changes is safe when each agent targets a different string in the same file — but count-based tests (test_command_count) must be updated by one agent after all changes land; run full regression after merge | STORY-043/044/045 |
| 2026-02 | Adding a new command requires 4 touch points: commands.py string, config.py VALID_COMMANDS, rules.py routing table, and count assertions in ~6 test files — data-driven count tests reduce future churn | STORY-043 |
| 2026-02 | Release hygiene requires syncing 5 artifacts (CHANGELOG, pyproject.toml, __init__.py, plugin repo, PyPI) — missing any one creates version drift; a release Story with explicit per-artifact tasks prevents omissions | STORY-014 |
| 2026-02 | Local PDCA must mirror CI checks — if CI runs lint, the Done command must too; adding `lint_command` to `LANG_PROFILES` and a conditional CI Lint Gate step closes the gap between local green and CI green | STORY-015 |
| 2026-02 | Constitution rules are the cheapest way to change agent behavior — adding a 3-line language-matching rule to core protocol makes all PDCA output respect user's language; project CLAUDE.md should be instruction-focused, not documentation | STORY-016 |
| 2026-02 | Project-level CLAUDE.md should be scaffolded by init — prompt-only changes to command templates are the cheapest way to add new init artifacts without touching runtime code | STORY-017 |
| 2026-02 | Architecture docs that are written once at Init (rules.md, system_design.mmd) drift silently — close the loop by adding verification/refresh steps to the Done command so staleness is caught every commit cycle | STORY-018 |
| 2026-02 | Expert critiques should be fact-checked against actual code before planning — Gemini flagged 4 issues but one was already handled; 3 prompt-only changes + 1 code change (visualize.py depth/max-nodes) addressed the valid concerns without over-engineering (no tree-sitter for v1) | STORY-019/020/021 |
| 2026-02 | Bailout protocols need decision trees, not flat rules — distinguishing project-internal modules from third-party packages prevents false stops in autonomous Sprint mode; the fix was 5 lines of prompt text with zero runtime code changes | STORY-022 |
| 2026-02 | Green tests alone don't guarantee quality — AI agents can write tautological or over-mocked tests that pass but verify nothing; adding a Test Quality Gate to the QA check phase closes this audit gap with a prompt-only change | STORY-023 |
| 2026-02 | Nested YAML structures (hooks) in agent frontmatter require PyYAML serialization — simple f-string concatenation breaks indentation; splitting fields into SIMPLE_OPTIONAL (flat) and NESTED (yaml.dump) categories keeps the deployer clean. Model default should be `inherit` not a specific model name, so PactKit works with any subscription tier | STORY-024 |
| 2026-02 | All new features must be conditional/opt-in by default — CI/CD, issue tracking, hooks, and lint blocking are all disabled by default and only activate when explicitly configured; this respects diverse user environments | STORY-025~030 |
| 2026-02 | Hook safety requires three rules: command-type only (no prompt/agent), report-only (exit 0), and opt-in (disabled by default) — prompt-type hooks add latency and risk infinite loops in PostToolUse patterns | STORY-027 |
| 2026-02 | Rule scoping via includeFiles frontmatter enables glob-targeted governance without forking rule modules — a single rule_scopes config dict maps rule IDs to glob patterns, and the deployer prepends YAML frontmatter at write time | STORY-028 |
| 2026-02 | Marketplace deployment puts PactKit files alongside project source — visualize must exclude deployed dirs (skills/commands/rules/agents) to avoid graph pollution; use case validation across 4 personas catches integration issues that unit tests miss | BUG-006/007, STORY-031/032 |
| 2026-02 | Command-to-skill demotion requires a full grep audit of all prompt templates — BUG-007 fixed /project-trace but missed 4 other demoted commands (review/release/doctor/draw) referenced in agents.py and skills.py; a comprehensive scan for all 6 demoted names at once would have caught them all in one pass | BUG-008 |
| 2026-02 | Documentation drift accumulates silently across config changes — BUG-013 changed pactkit.yaml path but the website was not updated; a systematic README vs. site audit found 23 gaps including stale rule descriptions and wrong field types; multi-repo docs require a sync checklist on every breaking change | STORY-036 |
| 2026-02 | Config file auto-merge must handle both list-type keys AND non-list sections — auto_merge_config_file only merged list items (agents/commands/skills/rules), leaving newly-added sections (hooks/ci/lint_blocking) invisible to users with old configs; _rewrite_yaml must also write all sections, not just lists | STORY-033 |
| 2026-03 | Regex quantifiers in f-strings need double braces (`{{3,4}}` not `{3,4}`) — single braces are interpreted as format placeholders; scaffold templates (create_board) prevent AI from generating inconsistent formats (#### vs ###) that break downstream regex matchers | BUG-027 |
| 2026-02 | Prompt-only Init Guard extensions are cheap but boundary detection in tests is tricky — when Phase 0.5 text references "Phase 1" inline (e.g., "resume from Phase 1"), tests using `content.index("Phase 1")` as section boundary will slice too early; use the full header pattern (e.g., "Phase 1: Archaeology") instead | STORY-034 |
| 2026-02 | Dual-config architecture (global ~/.claude vs project $CWD/.claude) means deployer must backfill both paths — auto_merge_config_file works on any path but was only called for the global config; adding a `_backfill_project_config()` that checks `Path.cwd()/.claude/pactkit.yaml` existence before calling auto_merge closes the gap without creating configs that don't exist | BUG-009 |
| 2026-02 | Documentation-only stories benefit from test-driven verification — writing 30 assertion tests for README/CHANGELOG content before editing ensures no required content is missed and catches drift in future releases | STORY-035 |
| 2026-02 | When adding new config sections, every serialization path must be updated — _rewrite_yaml, generate_default_yaml, and _BACKFILL_KEYS are three separate places that must stay in sync; agent_models and rule_scopes were validated and consumed but never serialized, causing silent data loss on every `pactkit update` | BUG-010 |
| 2026-02 | Command-to-skill demotion requires exhaustive grep across ALL prompt modules — BUG-007 fixed one reference, BUG-008 fixed four more, but BUG-011 found seven still remaining in agent protocol headers and skill Usage lines; a single `grep -r` across the entire prompts/ directory at demotion time would have caught all references in one pass | BUG-011 |
| 2026-02 | AST-based call graphs must filter noise at two levels: _extract_calls should skip builtins and non-self attribute calls, and _build_call_graph should only emit edges where callees resolve to func_registry — without both filters, a 20-function project generates 464 lines of noise; with both, it drops to 77 | BUG-012 |
| 2026-02 | Single-source config requires three coordinated changes: (1) read from CWD not HOME, (2) generate at CWD not HOME, (3) auto_merge must backfill entirely missing list keys (not just merge into existing lists) — tests using `target=` without mocking `Path.cwd()` will leak CI files to the real project directory | BUG-013 |
| 2026-02 | Internal prompt iteration versions (v16.2~v23.0) diverged silently from package versions — version hygiene requires a single grep audit across all prompts/ and skills/ at each release; cross-referencing CHANGELOG with spec Release fields catches phantom versions (1.1.5, 1.2.1) that were never actually released | BUG-014 |
| 2026-02 | Regression decision trees with unverifiable conditions (session freshness, ALL-match naming) become dead code — replace with git-diff-based checks and fallback thresholds to make incremental testing actually reachable | STORY-037 |
| 2026-02 | Shared output filenames across graph modes (focus_graph.mmd) cause silent overwrites — use mode-specific names (focus_file_graph.mmd, focus_class_graph.mmd, focus_call_graph.mmd) and ensure all three graph types are in the Update Reality cycle to prevent staleness | STORY-038 |
| 2026-02 | Playbook instructions must invoke CLI tools, not duplicate their logic — /project-init manually created 3-field pactkit.yaml while pactkit init CLI generates 70-line config; always delegate to single-source tooling and add CLI availability checks with fallback | BUG-017 |
| 2026-02 | Conditional steps marked "IF available" may be skipped by weaker models — adding a verification phase (Done 3.5.5) that backfills missing GitHub Issues provides a safety net; prompts should not assume earlier phases executed correctly | BUG-018 |
| 2026-02 | Virtual environment config must follow existing section patterns — detect_venv() for auto-discovery, venv in get_default_config(), _BACKFILL_KEYS, _rewrite_yaml(), generate_default_yaml(), validate_config() all need updating; prompt-based CLAUDE.md generation handles Claude's runtime behavior via project-init instructions | STORY-039 |
| 2026-02 | Config schema + function without deployment integration = dead code — detect_venv() existed but was never called in deployer; adding _generate_project_claude_md_if_missing() completes the venv feature by generating project-level CLAUDE.md with venv section when detected | BUG-019 |
| 2026-02 | Test isolation requires mocking both Path.home() AND Path.cwd() when testing deploy() — project-level file generation uses cwd while global files use home; without both patches, tests leak modifications to real project files | BUG-020 |
| 2026-02 | When later Specs override earlier Specs (BUG-021 supersedes BUG-020), the tests for the earlier Spec must be updated — Tier 1 hierarchy applies to code AND tests; leaving old tests unchanged causes failures that look like regressions but are actually intentional design changes | BUG-021 |
| 2026-02 | Nested dict config sections need deep merge at load time — shallow merge loses default sub-keys when user specifies partial config; a DEEP_MERGE_KEYS set handles venv/ci/hooks/issue_tracker while preserving shallow override for scalars and lists | BUG-022 |
| 2026-02 | Config rewriters must preserve unknown user-defined keys — _rewrite_yaml dropped custom keys silently; adding a KNOWN_KEYS set and writing unknown keys under a "# Custom" comment section provides round-trip safety for user extensions | BUG-023 |
| 2026-02 | Dual-file layered architecture separates framework (CLAUDE.md) and user (CLAUDE.local.md) content — enables safe regeneration while preserving user customizations; migration heuristic detects user-modified files by checking if content starts with expected project header | STORY-040 |
| 2026-02 | CLI tools need subprocess-based E2E tests in addition to unit tests — calling main() directly doesn't test entry point registration, argparse routing, or exit codes; e2e/cli/ with subprocess.run covers the full invocation path | STORY-041 |
| 2026-02 | Read-side regex tolerance (`\[?...\]?:?`) is simpler than format detection — board.py regex fix used optional brackets/colon in a single shared `_TITLE_RE` constant instead of branching logic, making all 4 parse sites accept both `### [ID] Title` and `### ID: Title` formats with minimal code change | BUG-024 |
| 2026-02 | Worktree isolation for subagent Sprint is a prompt-only change — adding `isolation="worktree"` to Task calls plus merge/copy recovery instructions eliminates parallel write conflicts without any Python code change; the key insight is that Stage A/C need git merge while Stage B only needs file copy for reports | STORY-048 |
| 2026-02 | GitHub Community Standards files are pure documentation but benefit from TDD — writing 17 content-assertion tests before creating CODE_OF_CONDUCT.md, SECURITY.md, and dependabot.yml ensures no required section is missed and catches future drift; using GitHub Security Advisory instead of personal email avoids PII exposure in public repos | STORY-049 |
| 2026-02 | Regression decision trees need a doc-only shortcut before any other heuristic — the Fast-Suite Shortcut (< 30s → FULL) masks the fundamental check "were any source files changed?"; adding Step 1.3 with `source_dirs` from LANG_PROFILES enables zero-cost doc-only commits even on large projects | STORY-050 |
| 2026-02 | Splitting an overloaded command (Done 13+ phases) into focused commands (Done + Release + PR) requires updating tests that verified the old behavior — all 21 pre-existing test failures were intentional Spec-driven changes; the key discipline is distinguishing "tests broken by our change" (update them, Spec wins) from "tests uncovering a regression" (stop and revert) | STORY-051 |
| 2026-02 | Command-to-skill promotion (project-release re-promoted in STORY-051) requires exhaustive audit of ALL stale-ref tests — BUG-025 exposed a missed `stale_cmds` entry in test_bug008 that still listed `/project-release`; when re-promoting a demoted command, grep ALL stale-ref tests for that command name | BUG-025 |
| 2026-02 | Adding a new config section requires 7 coordinated changes in config.py: get_default_config, DEEP_MERGE_KEYS, _BACKFILL_KEYS, KNOWN_KEYS, _rewrite_yaml write block, validate_config, and generate_default_yaml — plus 2 "full config" test fixtures that must be updated to include the new section | STORY-052 |
| 2026-02 | Impact-based reverse BFS (callee → callers) is a clean extension to an existing forward BFS — extract a shared `_scan_call_edges()` helper so both forward and reverse modes read source once; the `impact()` function maps callers to test files via stem-based pattern (`test_{stem}.py`), keeping the implementation at ~30 lines with no new dependencies | STORY-053 |
| 2026-02 | Deployment completeness tests must assert exact counts and exact names (not just `>= 1`) — loose guards like `len(files) >= 1` pass even when only 1 of 11 commands is deployed; importing `VALID_*` sets from config.py and asserting `set(found) == VALID_*` creates a self-updating contract that breaks on any future drift | STORY-054 |
| 2026-02-27 | Adding new config sections requires 7 coordinated config.py touch points (get_default_config, DEEP_MERGE_KEYS, _BACKFILL_KEYS, KNOWN_KEYS, _rewrite_yaml, validate_config, generate_default_yaml) plus 2 full-config fixture updates; when restructuring prose into a structured checklist, preserve pre-existing keyword tests by adding OWASP context notes below the table rather than modifying the tests (score: 22/25) | STORY-055 |
| 2026-02-27 | Version sync requires 4 coordinated changes in config.py: (1) `from pactkit import __version__`, (2) `get_default_config()` version → `__version__`, (3) `_rewrite_yaml()` always writes `__version__`, (4) `auto_merge_config_file()` version-staleness check before `if added:` to trigger rewrite even when nothing else changed — full-config fixture tests in 4 files must be updated to include `'version': __version__` (score: 21/25) | BUG-026 |
| 2026-02-27 | Extending an existing config section requires only 4 touch points (vs 7 for a new section): get_default_config dict literal, _rewrite_yaml write block, validate_config validation block, generate_default_yaml output block — no DEEP_MERGE_KEYS/_BACKFILL_KEYS/KNOWN_KEYS changes needed; for prompt-level filtering logic, add detection rules table directly in the prompt string rather than in Python code — zero runtime overhead, fully testable via string assertions (score: 21/25) | STORY-056 |
| 2026-02-28 | Playbook instructions that reference unobservable state (e.g., "if last test run < 30s") are dead code for LLMs — remove rather than try to make them work; when a decision tree uses High/Medium signal classification, always label signals explicitly in the definition list so LLM evaluation is deterministic across sessions | STORY-057 |
| 2026-02-28 | Routing table metadata claims ("Embedded In" column) must be verified against actual command playbook references — 5 of 7 skills had incorrect invocation claims that no command actually made; splitting into command-invoked vs agent-only tables prevents future misattribution and makes the LLM's skill routing decisions accurate (score: 19/25) | STORY-058 |
| 2026-03-02 | Adding a new section to an existing prompt string is a low-risk modification when insertion point and renumbering are explicit — Section 1.6 inserted after 1.5 with 1.6-1.9→1.7-2.0 renumbering required zero changes to Phase-level tests because Phase numbers (Phase 0-5) are independent from Section numbers (1.1-2.0); conditional MCP patterns (IF tool available → use, ELSE → skip) are reusable across Draw.io, Playwright, and future integrations (score: 18/25) | STORY-059 |
| 2026-03-02 | Three patterns for init-hang prevention: (1) Playbook interactive prompts block in non-interactive contexts (subagent, CI) — always provide a non-blocking fallback; (2) argparse flags parsed but not forwarded through the call chain are dead code that misleads users — wire every flag end-to-end or remove it; (3) unbounded rglob+ast.parse needs a file count ceiling (500) with a --focus escape hatch — removing **_kwargs requires adding explicit deprecated params (mode=None) for backward compat with existing callers (score: 19/25) | STORY-060 |
| 2026-03-02 | Explicit `<thinking>` block instructions in prompts are redundant when the LLM has native extended thinking — removing 10 instances across commands.py and workflows.py saves ~50-100 output tokens per invocation with zero behavior change; extends the STORY-008 pattern of removing rules that overlap with LLM native capabilities (score: 15/25) | STORY-061 |
| 2026-03-03 | Post-deployment recommendations (MCP servers) are a cheap discoverability win — adding a 6-entry constant + 5-line print helper surfaces optional features without blocking the user or adding runtime deps; conditional MCP integration instructions in rules already exist but users need to know what MCPs are worth installing | STORY-062 |
| 2026-03-04 | Pre-existing tests form a hard constraint map when slimming prompts — 150+ keyword assertions must be preserved verbatim even when removing prose; extracting duplicated logic (Lazy Visualize, Test Mapping, context.md format) into `rules.py` shared protocols eliminates 4-place duplication while keeping referenced keywords in playbooks; slim prose around keywords, not the keywords themselves (22.5% total reduction, 73K→57K chars) | STORY-063 |
| 2026-03-04 | Constants protected by Spec-mandated tests (test_stack_references.py, 33 tests from STORY-026) must NOT be removed even if deployer.py never uses them — the fix is to remove ghost NAME references from playbook prose, not the constants themselves; building a constraint map from ALL test keyword assertions before any prompt text change prevents cascade failures | BUG-028 |
| 2026-03-05 | Playbook fallbacks that "ask the user to specify" cause CLI hangs in slash commands — Claude Code cannot pause mid-flow for interactive input; always use config-first resolution (read pactkit.yaml stack value) with non-blocking auto default, never block on user input during command execution | BUG-029 |
| 2026-03-05 | Playbook prompts must never hardcode project-relative paths (e.g., `python3 src/pactkit/skills/spec_linter.py`) — these paths only exist inside the pactkit dev repo; always use the installed CLI entry point (`pactkit spec-lint`) so prompts work in any project that has pactkit installed | BUG-030 |
| 2026-03-05 | When a file is both pactkit-managed and user-customizable, use HTML comment markers (`<!-- pactkit:venv:start/end -->`) to partition the file into a pactkit-owned block (top) and user-owned region (below) — `_upsert_venv_managed_block()` in deployer.py replaces only the marked region via `re.sub(DOTALL)`, leaving user content untouched; this pattern allows pactkit update to persist venv paths across fresh checkouts where detection would otherwise fail | STORY-064 |
| 2026-03-05 | When adding content to a prompt with a hard char-size assertion (e.g., `SPRINT_PROMPT < 3000`), compute net delta = new_bytes - replaced_bytes before writing; Stage A split (A1/A2) actually shrank Stage A text by using a compact inline format while nesting new sub-stages under the existing `Stage A` heading so pre-existing `find('Stage A')` tests remain valid — slim the added prose to offset the net addition | STORY-065 |
| 2026-03-06 | When a new feature changes a file's lifecycle (e.g., STORY-064 `_upsert_venv_managed_block` now modifies `CLAUDE.local.md`), update all docstrings and template comments that describe the old lifecycle — stale "never modified" claims mislead future developers and break pre-existing assertion tests like `test_story040_layered_claude_md.py:59` | BUG-031 |
| 2026-03-13 | When adding a validation linter (spec_linter.py), update all code generators (scaffold.py::create_spec) to produce output that passes the new rules — E001 (metadata table), E004 (R{N} subsections), E008 (no TBD); test the generator output against the linter directly to catch format drift early | BUG-033 |
| 2026-03-16 | LLM playbooks that instruct "create a Spec file" must include an explicit metadata table template — without a visible example showing exact format (pipe-delimited Field/Value header, exact field names), the LLM generates inconsistent formats (bold field names, wrong headers) that fail downstream spec-lint; the fix is a 10-line template addition to commands.py Plan Phase 3 | BUG-034 |
| 2026-03-16 | Multi-format deployment (classic/plugin/marketplace/opencode) reuses existing helpers (_deploy_skills, _deploy_agents, _deploy_commands) with different path prefixes via `skills_prefix` parameter — adding a new format requires: (1) constant in VALID_FORMATS, (2) path prefix constant, (3) routing in deploy(), (4) format-specific _deploy_X() function, (5) format-specific JSON config generator; path rewriting at write time keeps templates canonical | STORY-069 |
| 2026-03-16 | Dual-layer deployment (global + project) must split file generation between CLI (`pactkit init`) and playbook (`/project-init`) — global deploys shared resources (agents/commands/skills), project-level config files (opencode.json) go in playbook; keeping helper functions (_deploy_opencode_json) callable but not invoked by global deploy maintains both separation of concerns and code reuse | BUG-035 |
| 2026-03-17 | When deploying to a different AI tool (OpenCode vs Claude Code), `opencode_format` flag in `_deploy_agents()`/`_deploy_commands()` must transform ALL format aspects — tools record, mode field, name removal, Claude-only field cleanup, model inherit omission, and command frontmatter (`allowed-tools` → `agent: build`); verifying against official docs (opencode.ai/docs/agents, /commands) catches gaps that Spec-to-code comparison alone misses | STORY-070 |
| 2026-03-17 | When the target tool lacks `@import` syntax (OpenCode vs Claude Code), use its native config mechanism (`opencode.json` `instructions: ["rules/*.md"]`) for modular rule loading instead of inlining everything into a single file; `_update_global_opencode_json()` with merge strategy preserves user-owned fields (`provider`) while adding tool-managed fields (`instructions`) — same principle as `CLAUDE.local.md` but achieved through JSON merge instead of file separation | STORY-071 |
| 2026-03-17 | Tool-internal config files (pactkit.yaml) must not be hardcoded to a single tool's directory — `load_config()` multi-path lookup (`.claude/` → `.opencode/`) with `find_pactkit_yaml()` helper + env-aware `resolve_pactkit_yaml_dir()` for generation keeps the CLI tool-agnostic; skill scripts (board.py) that run standalone cannot import library modules, so must inline the path lookup logic rather than calling `from pactkit.config import find_pactkit_yaml` | STORY-072 |
| 2026-03-17 | OpenCode command-level model routing requires `model:` in frontmatter (not just `agent.{name}.model` in opencode.json) — `_resolve_opencode_model_id()` maps shortnames (sonnet/opus/haiku) to full `provider/model-id` by scanning `opencode.json` providers; `command_models` in pactkit.yaml enables user override; also: any config YAML comments referencing `~/.claude/` must be tool-agnostic since OpenCode users see those comments in `.opencode/pactkit.yaml` | STORY-073 |
| 2026-03-17 | Tool integration checklists need a Dimension 0 capability matrix before any code — `docs/guides/tool-integration-checklist.md` shows that OpenCode (architecture-compatible with Claude Code) needed 6 stories while Codex (no agents, no rules, single provider) would require a completely different strategy; filling the matrix first prevents writing code that assumes the wrong architecture | STORY-slim-001 |
| 2026-03-17 | When format-specific logic is scattered across 10+ locations (deployer, config, prompts), use a frozen dataclass registry (`FormatProfile` in `profiles.py`) so adding a new tool format requires only one registry entry — functions read `profile.skills_path_var`, `profile.excluded_agent_fields`, etc. instead of checking `if format == "opencode"`; review caught unreachable duplicate code from copy-paste during migration (lines 59-61 in `_rewrite_skills_prefix`) | STORY-slim-005 |
| 2026-03-17 | Prompt templates with hardcoded env paths should use named placeholders (`{BOARD_CMD}`, `{SKILLS_ROOT}` etc.) resolved at deploy time by `_render_prompt(template, profile)`; use sequential `str.replace()` over `str.format_map()` because user-facing templates contain complex keys like `{R1, R2, ...}` and `{version}_*.mmd` that cause `ValueError: Empty attribute` in Python's format parser; f-string prompt constants must be converted to plain strings first | STORY-slim-006 |
| 2026-03-17 | Document structure rules should live in a single `schemas.py` registry — `spec_linter.py` and `scaffold.py` both had diverged copies of required fields/patterns; when standalone skill scripts (board.py, scaffold.py) cannot import the library, inline copies with a `# STORY-slim-007: canonical values in src/pactkit/schemas.py` comment; standalone script fallbacks in spec_linter.py's except ImportError block must be kept bit-for-bit identical to schemas.py values | STORY-slim-007 |
| 2026-03-17 | When two deploy functions share logic (classic/opencode), add missing functions one-by-one rather than creating a shared `_deploy_standard()` up-front — `_deploy_opencode()` was missing `auto_merge_config_file`, `_cleanup_legacy`, `_generate_project_agents_md`, and `_print_mcp_recommendations_opencode`; adding them separately is safer than a premature abstraction that could break the plugin/marketplace modes which use different parameter patterns (`_legacy_prefix`) | STORY-slim-008 |
| 2026-03-18 | OpenCode `instructions: ["rules/*.md"]` glob loads ALL rules every turn (no lazy loading); the workaround is to split `RULES_FILES` into `RULES_CORE_FILES` (always-load via instructions, 3 files), `RULES_ONDEMAND_FILES` (referenced via `@rules/xxx.md` in AGENTS.md, AI reads on-demand via Read tool), and `RULES_INSTRUCTIONS_CORE` (list including user-managed files like `09-credential-safety.md`); user-managed files MUST NOT be in `RULES_FILES` (which drives `_deploy_rules`) or they cause `KeyError` in `prompts.RULES_MODULES` lookup | STORY-slim-009 |
| 2026-03-17 | Architecture principles derived from project practice should be codified as a named rule module (08-architecture-principles.md) and deployed to all tool environments via `RULES_FILES`; the `CLAUDE_MD_TEMPLATE` auto-generate pattern (building `@import` list from `sorted(RULES_FILES.values())`) prevents new rules from being silently omitted from Classic mode while OpenCode picks them up via `instructions: ["rules/*.md"]` glob — a DRY fix exposed by adding the new module | chore/rules |
| 2026-03-19 | `deployer.py` 中 `rule_id_to_key` 反向映射构建重复 3 处（`_deploy_rules`/`_deploy_claude_md`/`_deploy_claude_md_inline`），提取为 `_build_rule_id_to_key()` 和 `_build_rule_id_to_filename()` 两个 helper；同理 `_deploy_skills` 中 skill_md 渲染逻辑重复 2 处，提取为 `_render_skill_md()` — 用 `inspect.getsource()` 做源码断言可确保不回退 | STORY-slim-010 |
| 2026-03-20 | Rule injection should be command-scoped, not global: `COMMAND_RULES_MAP` in `rules.py` maps each command to its needed rules, `_build_command_rules_header()` in `deployer.py` generates Classic @import or OpenCode inline headers; the credential safety rule (09) is force-injected via `_get_command_rules()` even when user config omits it; OpenCode inline headers must use `<!-- rules-end -->` separator instead of `---` to avoid ambiguity with YAML frontmatter `---` blocks | STORY-slim-011 |
| 2026-03-20 | CI pipeline generation (`_deploy_ci`) is project-level (writes `.github/workflows/`), not tool-format-level — both `_deploy_classic()` and `_deploy_opencode()` must call it; `CI_PROFILES` in `workflows.py` parallels `LANG_PROFILES` to avoid hardcoding stack-specific setup actions/images in templates; `actions_ref` prefix replacement uses string prepend (`{ref}actions/checkout@v4`) rather than regex to stay simple and YAML-injection-safe; explicit `ci.github_host` config takes priority over `_detect_ghe()` auto-detection so users without git remotes can still configure GHE mode | STORY-slim-012 |
| 2026-03-20 | `(Mandatory)` labels on Phase 0 headers cause LLM extended thinking loops — the label reinforces "complete all thinking before output", combining with dense MUST requirements (7+ in Plan Phase 3) to create cognitive overload; fix has two layers: (1) remove `(Mandatory)` + add `Execution Style: incremental output` directive, (2) split monolithic phases into sub-phases (3.1/3.2/3.3) with explicit output checkpoints between steps; extends STORY-008/061 pattern of removing prompt elements that conflict with LLM native behavior | STORY-slim-013 |
| 2026-03-21 | Migrating ~25 deterministic rules from MD prompts to Python CLI subcommands (8 new modules: guards.py, id_generator.py, cleaners.py, regression.py, context_gen.py, validators.py, sec_scope.py, lazy_visualize.py) — the hardest part is backward compatibility: 2500+ pre-existing tests assert specific keywords in prompt text, so slimmed prompts must preserve every keyword even when replacing verbose blocks with single CLI invocations; build a keyword constraint map from failing tests incrementally rather than trying to predict all constraints up-front | STORY-slim-014 |
| 2026-03-21 | Post-migration audits should compare MD-defined rules vs Python CLI implementations systematically — _CLEANUP_PATTERNS in cleaners.py diverged from LANG_PROFILES canonical source (missing .gradle/, extra *.class); prompt commands that delegate to CLI tools must reference ALL existing subcommands, not just the ones mentioned in the migration story; pre-existing keyword tests (e.g., "Reason", "N/A" in SEC scope) constrain even refactored prompt text | BUG-slim-003 |
| 2026-03-21 | Deterministic diagnostics (doctor.py, backfill.py, issue_sync.py) are pure set-diff/regex/mtime operations with zero AI judgment — extracting them to CLI modules follows the same pattern as STORY-slim-014 (guards, cleaners, validators); subprocess calls to `gh` CLI must use list-form args (not shell=True) to prevent command injection; when slimming prompt prose to delegate to CLI, preserve all keywords checked by pre-existing tests (e.g., "backfill", "gh issue create") | STORY-slim-015 |
| 2026-03-21 | Config resolution in CLI modules must use `find_pactkit_yaml(project_root)` → `load_config(yaml_path)` two-step pattern — `load_config(project_root)` alone fails because pactkit.yaml lives in `.claude/` subdirectory, not project root; lint_runner.py and test_mapper.py both reuse `LANG_PROFILES` from workflows.py as canonical source for stack-specific patterns (test_map_pattern, lint_command), keeping DRY with the CI pipeline profiles | STORY-slim-016 |
| 2026-03-21 | Cross-flow integrity audits (comparing all PDCA commands end-to-end) catch gaps that per-command reviews miss — CLI subcommands with zero prompt references are dead code (`lint-context`, `lint-lessons`, `lint-testcase`); argparser flags without `deploy()` signature parity (`upgrade` missing `--agent`) fail silently; adding a new parameter to `deploy()` must use a default value to avoid breaking 24+ existing callers that don't pass it | BUG-slim-004 |
| 2026-03-22 | Done Phase deterministic operations (lessons dedup, invariants regex, coverage parsing) are pure text/regex/subprocess with zero AI judgment — `lessons.py` uses Jaccard similarity on word sets (threshold < 0.5) for dedup, `invariants.py` uses `re.sub()` for test count replacement, `coverage_gate.py` uses list-form `subprocess.run()` for pytest-cov (SEC-7 safe); prompt delegation must preserve fallback instructions ("if pactkit X unavailable, fall back to manual") so LLM can still operate without CLI installed | STORY-slim-017 |
| 2026-03-22 | Iterative cross-flow audits find deeper gaps each pass — BUG-slim-004 found 6 gaps, BUG-slim-005 found 5 more; dict keys defined but never consumed (`LANG_PROFILES.test_dir/package_file/e2e_test_pattern`) are invisible to grep-based prompt audits because they only exist in data structures; board update instructions must reference `{BOARD_CMD} update_task` script (not vague "mark as done") for deterministic execution; every CLI subcommand must have at least one prompt reference or it's dead code (`lint-testcase` had zero) | BUG-slim-005 |
| 2026-03-21 | cli.py visualize --lazy was a decision-only tool that printed guidance but never called lazy_visualize.run_visualize_graphs(); always trace the execution path from CLI handler to actual side-effect to confirm end-to-end behavior | BUG-slim-006 |
| 2026-03-21 | 5 test files independently hardcoded LANG_PROFILES required keys — define canonical key sets as frozenset in schemas.py (LANG_PROFILE_REQUIRED_KEYS) so all consumers import from one source; when adding test assertions on dict keys, always check schemas.py first | STORY-slim-018 |
| 2026-03-23 | Monolithic prompt Phase 3.2 in commands.py caused AI thinking stalls; splitting into 4 sub-phases (3.2a-3.2d) with output checkpoints forces incremental generation and eliminates the bottleneck | src/pactkit/prompts/commands.py:COMMAND_PROMPTS[project-plan.md] |
| 2026-03-23 | code-explorer maxTurns reduced from 50 to 15 in agents.py; Plan Phase 1 now requires bounded Explore prompts with target/scope/limit/output via Delegation Template in commands.py | src/pactkit/prompts/agents.py:code-explorer.maxTurns,src/pactkit/prompts/commands.py:Phase1.DelegationTemplate |
| 2026-03-23 | Added 09-sectional-write as core rule in src/pactkit/prompts/rules.py RULES_MODULES. Threshold 300 lines based on P75-P90 analysis of specs/source/tests. Applies to any file type. | src/pactkit/prompts/rules.py:RULES_MODULES |
| 2026-03-23 | When adding a new pactkit.yaml config section, 4 touch points needed: get_default_config, DEEP_MERGE_KEYS, validate_config, generate_default_yaml. Table format in prompts saves chars vs individual sections. | config.py:get_default_config |
| 2026-03-24 | Plan Phase 3.2a scaffold-first: replaced AI freeform Write with {SCAFFOLD_CMD} create_spec + Read + Edit in src/pactkit/prompts/commands.py. Removed 3 inline format examples (~240 chars). Pre-existing tests (test_bug034, test_story055_commands, test_story_slim019) needed updates for new checkpoint wording. | src/pactkit/prompts/commands.py:Phase3.2a |
| 2026-03-24 | spec_linter.py _check_acceptance_criteria: per-subsection GWT check must use raw_text (with code blocks) not stripped text, because specs legitimately wrap Gherkin in fenced blocks | spec_linter.py:_check_acceptance_criteria |
| 2026-03-24 | Standalone scripts (visualize.py) need try/except ImportError guards for yaml.safe_load when reading pactkit.yaml — they cannot import from pactkit library | visualize.py:_load_scan_excludes |
| 2026-03-24 | Inline data in standalone scripts must have canonical-source comments pointing to the library module (e.g. _STACK_MARKERS → cleaners.py, _LANG_FILE_EXT → workflows.py) per Architecture Principle 1 | visualize.py:_STACK_MARKERS |
| 2026-03-24 | Worktree isolation diverges from working-tree: verify visualize.py _scan_files() signature is preserved across stories to avoid breaking callers | visualize.py:_scan_files |
| 2026-03-24 | Extracting _detect_stack() from _detect_file_ext() enables both file discovery and test mapping to share stack detection — DRY refactoring | visualize.py:_detect_stack |
