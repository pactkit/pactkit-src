# Lessons Learned

| Date | Lesson | Context |
|------|--------|---------|
| 2025-01 | Brand rename (ScafPy -> PactKit) touched 306 references — automate with `replace_all` | Migration |
| 2025-02 | Selective deployment needs cleanup of stale files when config changes | STORY-002 |
| 2025-02 | `pactkit.yaml` must be generated on first `init` with sensible defaults | STORY-001 |
| 2025-02 | Cross-session value comes from persistent artifacts (context.md, lessons.md), not from more rules — prompt changes are the cheapest high-impact mechanism | STORY-006 |
| 2025-02 | Adding a new command touches 3 files (config.py, commands.py, rules.py) plus count assertions in existing tests — keep count tests data-driven to reduce churn | STORY-007 |
| 2025-02 | Removing rules that overlap with LLM native behavior (55% token reduction) improves signal-to-noise — fewer rules = higher compliance on the ones that matter | STORY-008 |
| 2025-02 | Auto-merge new components via separate function (not load_config) preserves existing contract — exclude section in yaml handles user opt-out without version diffing | STORY-009 |
| 2025-02 | Release prep is a good time to catch stale numbers in docs — embed counts as tests to prevent future drift | STORY-010 |
| 2026-02 | Demoting commands to skills is a prompt-only refactor (no Python scripts needed for prompt-only skills) — but updating 25+ test files with hardcoded counts is the real cost; prefer data-driven assertions | STORY-011 |
| 2026-02 | Multi-repo docs sync is cheap via gh CLI + git clone/push — but tests reading deployed files (not source) can hide regressions until redeployment | STORY-012 |
| 2026-02 | Integrating an external MCP server is a prompt-only change — add conditional instructions to rules, skills, agents, and workflows; no runtime code needed. The conditional pattern (IF tool available → use it; ELSE → fallback) is now proven across 6 MCP integrations | STORY-013 |
| 2026-02 | Skill SKILL.md prompts must use absolute paths for script invocations — the LLM runs bash from project cwd, not the skill base directory; match the pattern already used in workflows.py and commands.py (~/.claude/skills/{name}/scripts/{script}.py) | BUG-001 |
| 2026-02 | Deploy-time path rewriting (template stays canonical, deployer rewrites at write time) is the correct pattern for multi-mode deployment — adding a simple `_rewrite_skills_prefix()` helper keeps templates DRY while supporting classic/plugin/marketplace modes with different path conventions | BUG-002 |
| 2026-02 | When iterating `ast.Import.names`, each alias must be processed individually — a `for` loop that overwrites a single variable silently drops all but the last import; collect into a list instead. Also deduplicate edges with a `seen` set to avoid Mermaid rendering issues | BUG-003 |
| 2026-02 | Dead code from refactors (`set(x)` as standalone expression) passes all tests because it's a no-op — use AST-based source inspection tests to catch dead code that linters miss in generated/deployed scripts | BUG-004 |
| 2026-02 | When two functions classify the same data (board stories), they must use the same logic — `archive_stories` used absence-of-todo as "done" while `_classify_story` required presence-of-done; align both to require `- [x]` for archival | BUG-005 |
| 2026-02 | Release hygiene requires syncing 5 artifacts (CHANGELOG, pyproject.toml, __init__.py, plugin repo, PyPI) — missing any one creates version drift; a release Story with explicit per-artifact tasks prevents omissions | STORY-014 |
| 2026-02 | Local PDCA must mirror CI checks — if CI runs lint, the Done command must too; adding `lint_command` to `LANG_PROFILES` and a conditional CI Lint Gate step closes the gap between local green and CI green | STORY-015 |
| 2026-02 | Constitution rules are the cheapest way to change agent behavior — adding a 3-line language-matching rule to core protocol makes all PDCA output respect user's language; project CLAUDE.md should be instruction-focused, not documentation | STORY-016 |
| 2026-02 | Project-level CLAUDE.md should be scaffolded by init — prompt-only changes to command templates are the cheapest way to add new init artifacts without touching runtime code | STORY-017 |
| 2026-02 | Architecture docs that are written once at Init (rules.md, system_design.mmd) drift silently — close the loop by adding verification/refresh steps to the Done command so staleness is caught every commit cycle | STORY-018 |
| 2026-02 | Expert critiques should be fact-checked against actual code before planning — Gemini flagged 4 issues but one was already handled; 3 prompt-only changes + 1 code change (visualize.py depth/max-nodes) addressed the valid concerns without over-engineering (no tree-sitter for v1) | STORY-019/020/021 |
| 2026-02 | Bailout protocols need decision trees, not flat rules — distinguishing project-internal modules from third-party packages prevents false stops in autonomous Sprint mode; the fix was 5 lines of prompt text with zero runtime code changes | STORY-022 |
| 2026-02 | Green tests alone don't guarantee quality — AI agents can write tautological or over-mocked tests that pass but verify nothing; adding a Test Quality Gate to the QA check phase closes this audit gap with a prompt-only change | STORY-023 |
| 2026-02 | Nested YAML structures (hooks) in agent frontmatter require PyYAML serialization — simple f-string concatenation breaks indentation; splitting fields into SIMPLE_OPTIONAL (flat) and NESTED (yaml.dump) categories keeps the deployer clean. Model default should be `inherit` not a specific model name, so PactKit works with any subscription tier | STORY-024 |
| 2026-02 | All new features must be conditional/opt-in by default — CI/CD, issue tracking, hooks, and lint blocking are all disabled by default and only activate when explicitly configured; this respects diverse user environments | STORY-025~030 |
| 2026-02 | Hook safety requires three rules: command-type only (no prompt/agent), report-only (exit 0), and opt-in (disabled by default) — prompt-type hooks add latency and risk infinite loops in PostToolUse patterns | STORY-027 |
| 2026-02 | Rule scoping via includeFiles frontmatter enables glob-targeted governance without forking rule modules — a single rule_scopes config dict maps rule IDs to glob patterns, and the deployer prepends YAML frontmatter at write time | STORY-028 |
| 2026-02 | Marketplace deployment puts PactKit files alongside project source — visualize must exclude deployed dirs (skills/commands/rules/agents) to avoid graph pollution; use case validation across 4 personas catches integration issues that unit tests miss | BUG-006/007, STORY-031/032 |
| 2026-02 | Command-to-skill demotion requires a full grep audit of all prompt templates — BUG-007 fixed /project-trace but missed 4 other demoted commands (review/release/doctor/draw) referenced in agents.py and skills.py; a comprehensive scan for all 6 demoted names at once would have caught them all in one pass | BUG-008 |
| 2026-02 | Documentation drift accumulates silently across config changes — BUG-013 changed pactkit.yaml path but the website was not updated; a systematic README vs. site audit found 23 gaps including stale rule descriptions and wrong field types; multi-repo docs require a sync checklist on every breaking change | STORY-036 |
| 2026-02 | Config file auto-merge must handle both list-type keys AND non-list sections — auto_merge_config_file only merged list items (agents/commands/skills/rules), leaving newly-added sections (hooks/ci/lint_blocking) invisible to users with old configs; _rewrite_yaml must also write all sections, not just lists | STORY-033 |
| 2026-02 | Prompt-only Init Guard extensions are cheap but boundary detection in tests is tricky — when Phase 0.5 text references "Phase 1" inline (e.g., "resume from Phase 1"), tests using `content.index("Phase 1")` as section boundary will slice too early; use the full header pattern (e.g., "Phase 1: Archaeology") instead | STORY-034 |
| 2026-02 | Dual-config architecture (global ~/.claude vs project $CWD/.claude) means deployer must backfill both paths — auto_merge_config_file works on any path but was only called for the global config; adding a `_backfill_project_config()` that checks `Path.cwd()/.claude/pactkit.yaml` existence before calling auto_merge closes the gap without creating configs that don't exist | BUG-009 |
| 2026-02 | Documentation-only stories benefit from test-driven verification — writing 30 assertion tests for README/CHANGELOG content before editing ensures no required content is missed and catches drift in future releases | STORY-035 |
| 2026-02 | When adding new config sections, every serialization path must be updated — _rewrite_yaml, generate_default_yaml, and _BACKFILL_KEYS are three separate places that must stay in sync; agent_models and rule_scopes were validated and consumed but never serialized, causing silent data loss on every `pactkit update` | BUG-010 |
| 2026-02 | Command-to-skill demotion requires exhaustive grep across ALL prompt modules — BUG-007 fixed one reference, BUG-008 fixed four more, but BUG-011 found seven still remaining in agent protocol headers and skill Usage lines; a single `grep -r` across the entire prompts/ directory at demotion time would have caught all references in one pass | BUG-011 |
| 2026-02 | AST-based call graphs must filter noise at two levels: _extract_calls should skip builtins and non-self attribute calls, and _build_call_graph should only emit edges where callees resolve to func_registry — without both filters, a 20-function project generates 464 lines of noise; with both, it drops to 77 | BUG-012 |
| 2026-02 | Single-source config requires three coordinated changes: (1) read from CWD not HOME, (2) generate at CWD not HOME, (3) auto_merge must backfill entirely missing list keys (not just merge into existing lists) — tests using `target=` without mocking `Path.cwd()` will leak CI files to the real project directory | BUG-013 |
| 2026-02 | Internal prompt iteration versions (v16.2~v23.0) diverged silently from package versions — version hygiene requires a single grep audit across all prompts/ and skills/ at each release; cross-referencing CHANGELOG with spec Release fields catches phantom versions (1.1.5, 1.2.1) that were never actually released | BUG-014 |
| 2026-02 | Regression decision trees with unverifiable conditions (session freshness, ALL-match naming) become dead code — replace with git-diff-based checks and fallback thresholds to make incremental testing actually reachable | STORY-037 |
| 2026-02 | Shared output filenames across graph modes (focus_graph.mmd) cause silent overwrites — use mode-specific names (focus_file_graph.mmd, focus_class_graph.mmd, focus_call_graph.mmd) and ensure all three graph types are in the Update Reality cycle to prevent staleness | STORY-038 |
