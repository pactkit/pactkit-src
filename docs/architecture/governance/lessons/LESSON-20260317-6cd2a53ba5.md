---
schema_version: 1
id: LESSON-20260317-6cd2a53ba5
date: '2026-03-17'
story_id: STORY-070
context: STORY-070
tags:
- legacy-import
legacy_source: docs/architecture/governance/archive/lessons_archive_202603.md
---

When deploying to a different AI tool (OpenCode vs Claude Code), `opencode_format` flag in `_deploy_agents()`/`_deploy_commands()` must transform ALL format aspects — tools record, mode field, name removal, Claude-only field cleanup, model inherit omission, and command frontmatter (`allowed-tools` → `agent: build`); verifying against official docs (opencode.ai/docs/agents, /commands) catches gaps that Spec-to-code comparison alone misses
