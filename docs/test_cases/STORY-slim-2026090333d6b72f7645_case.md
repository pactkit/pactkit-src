# Test Cases: STORY-slim-2026090333d6b72f7645 — SDLC 标准对齐 T1

> 实现位置:`tests/unit/test_story_2026090333_sdlc_t1.py`(R1-R6)与
> `tests/unit/test_spec_preflight.py::TestTableInlineRange`(preflight 修复)。

## TC-1: ADR 创建含必需结构 (R1, AC1)

**Given** 空项目根
**When** `create_adr(1, title)` 
**Then** `docs/architecture/governance/adr/ADR-0001-{slug}.md` 生成，含全部元数据行（ID/Status/Date/Supersedes/Superseded-by）与四必需节，Status=proposed
**Impl** `TestADRSCaffold::test_create_adr_writes_required_structure`

## TC-2: Supersession 回写 (R1, AC2)

**Given** ADR-0001 已存在
**When** `create_adr(2, ..., supersedes=ADR-0001-stem)`
**Then** 新 ADR 声明 Supersedes；旧 ADR 被增量回写 Superseded-by（其余内容未破坏）
**And** `lint_adr` 对"声明 Supersedes 但目标无回写"报错；Status=superseded 但 Superseded-by=None 报错
**Impl** `test_create_adr_supersedes_backwrites_target` / `TestADRLint::test_supersession_requires_back_reference`

## TC-3: ADR schema 与 lint (R1, AC1)

**Given** SCHEMA_REGISTRY["adr"] 与 ADR_STATUSES
**When** 检查注册与校验
**Then** 四必需节在 required_sections；statuses = proposed/accepted/superseded；缺节/非法 status 均 FAIL
**Impl** `TestADRSchema::*` / `TestADRLint::test_missing_section_fails` / `test_invalid_status_fails`

## TC-4: Key Decisions 聚合 (R1, AC1-2)

**Given** adr/ 目录含 superseded 的 ADR-0001 与 accepted 的 ADR-0002
**When** `_parse_last_adrs`
**Then** 聚合 ADR-0002、排除 ADR-0001
**Impl** `TestADRContextAggregation::test_key_decisions_aggregates_accepted_excludes_superseded`

## TC-5: Validation 语义 (R2, AC3)

**Given** check contract 与 project-check playbook
**When** 检查 invariants 与 Phase 5
**Then** contract 含 "user-path validation" invariant；Phase 5 含 Validation 表（real end-user path，特权捷径 = FAIL）
**Impl** `TestCheckValidationSemantics::*`

## TC-6: Postmortem 三条件触发 (R3, AC4)

**Given** project-hotfix playbook
**When** 检查 Phase 3.7
**Then** 含 Phase 3.7、user-visible/recurrence/data-risk 三触发、postmortems 产物路径、Board 回流 MUST、SKIP 理由记录
**Impl** `TestPostmortemDiscipline::*`

## TC-7: write-safety guide 可路由 (R4, AC5)

**Given** GUIDE_DEFINITIONS 注册 write-safety.md
**When** 渲染 guide 并检查 engineering index
**Then** 含 merge-over-replace 决策矩阵、litmus test、no-dual-write；index keyword 行 + Act 路由行均存在（孤儿不变量强制）
**Impl** `TestWriteSafetyGuide::*` + `TestEveryDeployedGuideIsRoutable`(存量不变量)

## TC-8: Knowledge Provenance 三时点 (R6, AC7)

**Given** capability-design capsule、act Phase 3、check Phase 2
**When** 检查三者
**Then** capsule 含来源优先级 + unverified 标注 + 编造=缺陷；act 含先验证后写；check 含 fabrication 信号（F821/undefined 归类为编造检测）
**Impl** `TestKnowledgeProvenance::*`

## TC-9: Adapter parity (R5, AC6)

**Given** 四 FORMAT_PROFILES
**When** `_render_prompt` 渲染 check/hotfix/act 命令
**Then** Validation/fabricat/Phase 3.7/verify 语义四端一致存活
**Impl** `TestAdapterParityT1::test_new_semantics_render_across_formats`

## TC-10: preflight inline-range（Act 时补充授权项）

**Given** Spec 的 Implementation Inputs 表行含 `src/mod.py:L2-L4`（或正文 backtick 同格式）
**When** `run_spec_preflight`
**Then** range 被拆出、按行摘录、不再报 required input does not exist——正则 `-L?` 修复使 L1-L2 格式首次真正可匹配（原为静默死代码，零测试覆盖）
**Impl** `test_spec_preflight.py::TestTableInlineRange::test_table_row_with_inline_range_loads_lines` / `test_backtick_reference_with_range_also_loads`
