# Test Cases: STORY-slim-2026082381e832771d4e — Workflow 终止门禁与宿主续跑

| Field | Value |
|---|---|
| Spec | STORY-slim-2026082381e832771d4e |

## TC-1: 未完成 run 拒绝 final（AC1）

- **Given** workflow 尚未到最终 checkpoint
- **When** 执行 finish guard
- **Then** 返回 continue_current_turn、下一步骤和非零退出码，且状态不被修改

## TC-2: 仅完成或真实外部阻塞可终止（AC2）

- **Given** completed、合法 blocked、空 blocker 与损坏状态
- **When** 执行 finish guard
- **Then** 仅前两种可结束，其他状态 fail closed

## TC-3: Managed 写入验证 run ownership（AC3）

- **Given** 缺失、错误 workflow、错误步骤或 Story 冲突的 run
- **When** 执行关键写入
- **Then** 在目标变化前拒绝操作，合法重试只验证不覆盖

## TC-4: Standalone 调用保持兼容（AC4）

- **Given** 用户显式选择 standalone 模式
- **When** 独立调用 scaffold 或 board
- **Then** 保持行为并报告 managed=false

## TC-5: 支持 hook 的宿主有界续跑（AC5）

- **Given** 宿主支持 completion hook 和 session re-entry
- **When** 未完成 run 尝试 final
- **Then** runner 恢复同一 run，attempt 单调增加且不重放副作用

## TC-6: 无 hook 时报告真实能力（AC6）

- **Given** adapter 不支持自动重入
- **When** 查询 capability
- **Then** 返回 auto_resume_available=false 与人工恢复命令

## TC-7: 无进展、并发与超限安全停止（AC7）

- **Given** digest 重复、owner 竞争或 attempt 超限
- **When** runner 继续调度
- **Then** workflow 安全 blocked，不形成无限循环

## TC-8: 人工副作用边界（AC8）

- **Given** 操作为 commit、archive、tag、publish、release、push 或 PR
- **When** host runner 请求自动跨越
- **Then** 返回 await_user，未经新授权不执行命令

## TC-9: Agent-loop 拒绝工具后提前 final（AC9）

- **Given** agent 在中间工具调用成功后生成阶段总结
- **When** harness 对照真实 workflow state
- **Then** final 不被接受，完成全部 checkpoint 后才被接受

## TC-10: 四平台部署保留协议（AC10）

- **Given** Classic、OpenCode、Codex 与 Copilot 隔离部署
- **When** 检查实际生成文件和 capability manifest
- **Then** 协议完整且保证等级与宿主能力一致

## TC-11: 十二个 Project 命令全部受管（AC11）

- **Given** 12 个内置 project-* 命令
- **When** 检查 registry、canonical templates 与中间 checkpoint
- **Then** 全部 persistence=full，模板包含 start/resume、checkpoint、finish guard，提前 final 被拒绝

## TC-12: Done 与发布命令不越过人工授权（AC12）

- **Given** Done 只完成审计或 Release/PR 即将产生外部副作用
- **When** 执行 finish guard 或 host continuation
- **Then** 未到最终边界不可结束，人工操作未授权时 await_user，完成验证后返回 done

## TC-13: 通用完成声明不可伪造（AC11）

- **Given** 任一 Check、Done、Init、Sprint、Hotfix、Design、Clarify、Release、PR 或 Debug workflow
- **When** 最终 checkpoint 只提交 `completion=verified`，没有该命令要求的结构化证据
- **Then** validator 拒绝完成；只有测试结果、治理状态、发布或诊断等命令级证据满足 contract 才能完成

## TC-14: 非团队平台仍部署 Sprint 入口（AC10, AC11）

- **Given** Codex、OpenCode 或 Copilot 不提供 Claude TeamCreate 编排能力
- **When** 部署全部 canonical `project-*` 命令
- **Then** `project-sprint` 仍被部署并保留 Managed Workflow；执行时按 Plan → Act → Check → Close 串行降级，不伪装并行执行
