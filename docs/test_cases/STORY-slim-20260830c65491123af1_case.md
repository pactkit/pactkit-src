# Test Cases: STORY-slim-20260830c65491123af1 — Gate telemetry + audit redaction

## TC-1: 落盘前脱敏 (R1, AC1)

**Given** 含 `PGPASSWORD=s3cret psql …` 的命令到达 secrets gate
**When** block 记录与 gate_blocked 事件写入
**Then** enforcement JSON 与事件 JSONL 均不含 `s3cret`，均含 `[REDACTED:password-literal]`
**And** 用户名（`-U chatbi`）不是凭据，不被脱敏；干净文本原样保留
**And** stderr 提示命名模式种类但不重复字面量；auth gate 的 reason/stderr 同样脱敏

## TC-2: 授权对计数真实流量 (R2, R5, AC2)

**Given** 默认 enforcement 配置
**When** `gh release create` 被拦截，随后 `pactkit gate authorize release` 执行
**Then** 事件流含 `authorization_asked` 及其后的 `authorization_granted`
**And** `collect_gate_telemetry` / `pactkit stats --format json` 显示 asked≥1、granted≥1

## TC-3: per-gate 拦截计数 (R3, AC3)

**Given** secrets gate 拦截两次、auth gate 拦截一次（含 push gate 拦截一次的独立场景）
**When** `pactkit stats` 运行
**Then** gate_blocks 显示 `secrets_gate: 2, auth_gate: 1`（及 `push_gate: 1`）

## TC-4: Skill 调用遥测 (R4, AC4)

**Given** 已部署 Skill matcher 的项目
**When** hook 收到 `{"tool_name": "Skill", "tool_input": {"command": "project-plan"}}`
**Then** `command_invoked` 事件（detail.command=project-plan）落盘，hook exit 0
**And** stats 显示 `project-plan: 2`（两次调用）、`project-act: 1` 等调用计数
**And** install_hook 注册的 matchers 含 `Skill`

## TC-5: 历史记录一次性擦洗 (R1, AC5)

**Given** reason 含字面量凭据的 enforcement JSON
**When** `pactkit clean`（scrub_enforcement_records）运行
**Then** reason 重写为 `[REDACTED:…]`，其余字段原样，文件仍为合法 JSON
**And** 无凭据的记录字节不变；损坏 JSON 被跳过不抛错

## TC-6: 遥测故障不阻塞判定 (SEC-7)

**Given** 事件流写入失败（如目录不可写）
**When** gate 拦截路径执行
**Then** enforcement 记录与拦截判定不受影响（record_gate_event 全程 try/except 静默）
