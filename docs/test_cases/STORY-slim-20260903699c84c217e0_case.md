# Test Cases: STORY-slim-20260903699c84c217e0 — 部署账本生命周期集成测试

> 实现位置:`tests/unit/test_deploy_ledger_lifecycle.py`(6 用例)。

## TC-1: 四格式闭环 (R1, AC1)

**Given** classic/opencode/copilot 各格式布局的临时 root
**When** 三段闭环（写 v1→记账→重建账本→写 v2→断言 in-place→写 v3→再断言）
**Then** 每轮零 .pactkit-new；重建后 files 映射含该路径且 digest == 磁盘内容（连跑 3 轮纪律）
**Impl** `test_record_rebuild_prove_three_rounds[classic|opencode|copilot]`（参数化）

## TC-2: opencode 后处理形态 (R2, AC2)

**Given** 写入后字节变换再记账再重建的 opencode root（当日缺陷 2+3 合成复现）
**When** 下轮写入变更内容
**Then** in-place 覆盖、零候选、内容为新版本（跑步机形态不可复现）
**Impl** `test_opencode_post_transform_flavor`

## TC-3: codex 合并视图 (R3, AC3)

**Given** references 表记账 + write_deploy_manifest 重建后的 codex root
**When** 以 {**previous_hashes, **read_command_references} 合并视图做所有权检查并写入变更
**Then** 合并视图 digest 跨重建存活；变更 in-place 零候选
**Impl** `test_codex_merged_reference_view`

## TC-4: 布局枚举回归 (R1)

**Given** 三格式各自布局的文件
**When** pactkit_owned_files 枚举
**Then** classic(skills/)/opencode(commands/)/copilot(prompts/) 全部入 files 映射（flavor-3 回归锚）
**Impl** `test_files_map_survives_rebuild_for_all_formats`
